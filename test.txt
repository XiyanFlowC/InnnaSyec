#include config.txt

#ifdef JP_VER ; for jp edi

#include jpdefs.txt

#elifd JPB_VER ; for jp best edi

#include bestdefs.txt

#elifd AM_VER ; for USA edi

#include nisdefs.txt

#elifd EU_VER ; for Europe edi

#include 505defs.txt

#elifd DBG_VER ; for NISA Prototype(Preview code)

#include dbgdefs.txt

#endif

; codes:
.offset ff000
.galign 8

.vma printf_loc
printf:

#ifdef REPLACE_DEBUG_PRINTF
.vma hprintf_loc
hprintf:
  j printf
  nop
#endif
#ifdef REPLACE_DEBUG_PUTS
.vma hputs_loc
hputs:
  dmove a1, a0
  j printf
  la a0, puts_str
puts_str:
  .asciiz "%s\n"
#endif